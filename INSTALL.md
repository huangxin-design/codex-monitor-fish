# 给 Codex 的自动部署说明

用户想要的结果是：自动安装 **Codex 监控小鱼**，打开项目选择网页，用户选择项目后开始查看本机 Token 记录。把安装步骤交给 Codex，最终交付可用网页。

## Windows 10 / 11（64 位）

1. 检查系统与当前仓库来源；本项目仓库为 `https://github.com/huangxin-design/codex-monitor-fish`。已有安装时继续使用同一用户的数据目录，保留项目选择。头像存于浏览器，同一浏览器与地址下保留；更换浏览器或端口后可能需要重新选择。
2. 从该仓库的最新正式 Release 获取版本标签（`v` 开头的三段版本号）。下载并阅读该标签下的 `tooling/install_windows.ps1`，使用与脚本相同的 `-ReleaseTag`，不要直接把网络返回内容传给 `Invoke-Expression`。
3. 执行已检查的本地脚本。例如将实际下载路径和已确认的版本代入：

   ```powershell
   powershell.exe -NoProfile -ExecutionPolicy Bypass -File "<本地脚本路径>" -ReleaseTag "<已确认的版本标签>" -NoBrowser
   ```

   这个 `ExecutionPolicy` 参数仅作用于本次进程，不更改系统设置。脚本会从固定仓库下载 Windows 程序及 SHA-256 文件，核对后安装到当前用户的 `%LOCALAPPDATA%\CodexMonitorFish\app\<版本>`，启动服务并输出 JSON。无需 Python、管理员权限、API Key 或安装 Skill。
4. 读取脚本输出的 `url`，确认是 `http://127.0.0.1:<端口>/`，并检查 `/api/health` 返回的应用标识为 `codex-monitor-fish-desktop`。优先使用宿主的浏览器面板打开这个地址；没有面板时打开系统默认浏览器。也可省略 `-NoBrowser`，让安装器自动打开浏览器。
5. 首次展示项目选择页，交由用户选择，不默默选定或扩大统计范围。已配置过会恢复上次看板，可点击 **切换项目** 重新选择。新任务会随刷新自动出现。

## 出错时

- 下载失败或校验不一致：停止执行下载文件，报告具体原因；不要跳过校验。
- 找不到项目：保留项目选择页的空状态，说明需要本机 Codex 使用记录。只有确认数据目录迁移后，才在页面的 **没有找到项目？** 中设置实际目录。
- 已运行旧版本：保留原设置；如安装器提示版本冲突，让用户在旧网页点 **退出监控** 后重试，不终止不明进程。
- 系统禁止运行未签名程序：如实说明限制，不关闭系统防护。当前发布文件未签名。
- macOS / Linux：目前没有免运行环境的安装包。若本机已有 Python 3.10+，可下载仓库源码并运行 `python3 desktop/runtime.py`，同样自动打开项目选择页；缺少 Python 时说明缺失条件，不宣称自动安装完成。

## 部署边界与验收

- 只安装这一个本地工具，不修改 Codex 设置、模型配置、登录凭据或开机启动项。用户的会话记录、CSV、路径和头像均留在本机。
- 普通安装使用默认用户目录；`-InstallDir`、`-NoLaunch` 是测试或明确指定位置时的选项，不需要让普通用户配置。
- 只有校验通过、服务健康检查通过、网页能打开后，才报告安装完成。没有记录时如实显示空状态，不能用示例数据代替。
- 数字是本机日志可见的历史 Token 处理量，不是订阅剩余额度或账单。监控刷新不调用模型；请勿另建周期性 Codex 任务。

脚本与发布文件使用 HTTPS 下载；SHA-256 用于校验文件一致性，不等同于代码签名或第三方安全认证。
