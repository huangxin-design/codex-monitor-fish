# 贡献说明

欢迎改进日志兼容性、统计准确性、界面与可访问性。提交时说明具体触发条件、改动后的行为和验证方法。

## 本地验证

使用 Python 3.10+，程序没有第三方运行依赖：

```text
python -m unittest discover -s skills/codex-token-monitor/assets/app -p "test_*.py"
python -m unittest discover -s tests -p "test_*.py"
python tooling/build_release.py
```

计数修改至少覆盖重复请求、继承历史、父子归并、范围选择和缺失记录。使用合成数据建立案例，不提交用户的原始日志、数据库或真实任务名称。

界面修改请检查 1440px 桌面和 390px 窄屏，验证合计可见、单位切换、CSV、个人头像上传和刷新状态。数据展示仍需使用文字节点，不能将日志中的名称直接拼入 HTML。

## 维护边界

- 本项目对已观测的本地记录进行解析，不声称这些内部格式是稳定接口。
- 无法支持的结构应明确报错。不要把旧累计快照当作精确用量兜底。
- 保持仅本地监听和只读源数据，避免引入上传或登录需求。
- Skill 模板与用户生成的运行目录分开；更新程序不要覆盖使用者配置。
- 图片与演示数据的授权需要单独检查；不要假定代码许可自动包含所有品牌素材。
