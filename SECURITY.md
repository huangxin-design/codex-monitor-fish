# 问题报告说明

本地任务名称、目录、日志和头像可能含私人信息。提交 Issue 时只提供经过脱敏的错误信息、Python/操作系统版本，以及最小合成复现案例；不要附上 `auth.json`、完整数据库、原始会话日志、真实 CSV 或账号凭据。

若发现可能暴露数据的问题，请优先使用仓库的私密漏洞报告功能（若仓库所有者已开启）。未开启时，可先提交不含利用细节或私人数据的问题，请维护者提供合适的沟通方式。本项目尚未承诺固定的安全响应时限。

当前默认边界为本机 `127.0.0.1`，接口不提供跨域授权，不修改源数据库。不要为远程访问直接绑定公网地址；这不是带账户认证的多用户服务。
